/**
 * Site-wide SEO settings. SITE_URL must match the live domain exactly
 * (no trailing slash) — it is used for canonical links and social previews.
 * Keep in sync with index.html, public/robots.txt and public/sitemap.xml.
 */
export const SITE_URL = "https://dsdsecurityservices.in";
export const OG_IMAGE = `${SITE_URL}/og-image.jpg`;
